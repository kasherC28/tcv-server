export enum Role {
  USER = 1,
  ADMIN = 2
}

export enum UserStatus {
  ACTIVE = 1,
  BLOCKED = 2
}

export enum Gender {
  M = 1,
  F = 2
}

export enum ContactTopic {
  ORDER_INQUIRY = 1,
  IRRELEVANCE = 2,
  TECHNICAL_ISSUE = 3,
  GENERAL_QUESTION = 4,
  FEEDBACK = 5
}

export enum CheckoutStatus {
  PENDING = 1,
  CONFIRMED = 2,
  REJECTED = 3
}
